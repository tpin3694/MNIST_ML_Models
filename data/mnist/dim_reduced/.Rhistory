Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_few()
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
source('~/.active-rstudio-document', echo=TRUE)
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
knitr::opts_chunk$set(echo = TRUE, cache = TRUE, fig.align = "center")
library(data.table, warn.conflicts = F)
library(mice, warn.conflicts = F)
library(dplyr, warn.conflicts = F)
library(ggplot2, warn.conflicts = F)
trans_del
View(train)
setwd("/home/tpin3694/Documents/university/MSc/fundamental/THG-Jarvis/")
del_key <- fread("delivery_option_lookup.csv")
trans_del <- trans %>%
left_join(del_key) %>%
group_by(Name) %>%
summarise(n = n())
colnames(del_key) <- c("Delivery_Option_Type_Key", "Name")
trans_del <- trans %>%
left_join(del_key) %>%
group_by(Name) %>%
summarise(n = n())
trans_del <- trans %>%
left_join(del_key) %>%
group_by(Name) %>%
summarise(n = n())
train <- train %>%
left_join(del_key, by = "Delivery_Option_Type_Key")
colnames(del_key)
del_key
del_key <- fread("delivery_option_lookup.csv")
colnames(del_key) <- c("Delivery_Option_Type_Key", " Deliver_Name")
trans_del <- trans %>%
left_join(del_key) %>%
group_by(Deliver_Name) %>%
summarise(n = n())
train <- train %>%
left_join(del_key, by = "Delivery_Option_Type_Key")
colnames(del_key) <- c("Delivery_Option_Type_Key", "Delivery_Name")
trans_del <- trans %>%
left_join(del_key) %>%
group_by(Delivery_Name) %>%
summarise(n = n())
train <- train %>%
left_join(del_key, by = "Delivery_Option_Type_Key")
# Build model
attach(train)
del_join
p <- trans_del %>%
ggplot(aes(x = Name, y = log(n))) +
geom_col() +
theme(axis.text.x = element_text(angle = 60, hjust = 1))
plot(p)
customers <- trans %>%
group_by(Account_Key) %>%
mutate(count = n()) %>%
group_by(count) %>%
summarise(n = n())
customers$prop <- customers$n/dim(trans)[1]
customers$cumulate <- cumsum(customers$prop)
customers %>%
ggplot(aes(x = count, y = cumulate)) +
geom_line() +
scale_x_continuous(breaks = seq(0, 800, by = 25)) +
scale_y_continuous(breaks = seq(0, 800, by = 0.1)) +
theme_minimal()
payment_hists <- trans %>%
group_by(Account_Key) %>%
mutate(pay_key_num = as.numeric(Order_Payment_Status_Key),
num_valid = length(which(pay_key_num == 0)),
count = n(),
prop = num_valid/count,
status = ifelse(prop <= 0.25 & count > 2, "questionable",
ifelse(prop <= 0.25 & count <= 2, "insufficient",
ifelse(prop > 0.25 & prop < 0.75, "adequate", "fine"))))
payment_hists %>%
ggplot(aes(x = status)) +
geom_bar(stat = "count")
payment_hists
train <- train %>%
group_by(Account_Key) %>%
mutate(pay_key_num = as.numeric(Order_Payment_Status_Key),
num_valid = length(which(pay_key_num == 0)),
count = n(),
prop = num_valid/count,
status = ifelse(prop <= 0.25 & count > 2, "questionable",
ifelse(prop <= 0.25 & count <= 2, "insufficient",
ifelse(prop > 0.25 & prop < 0.75, "adequate", "fine"))))
source('~/Documents/university/MSc/fundamental/fraud/R/fraud_rf.R')
colnames(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + Delivery_Name+status)
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + Delivery_Name+status)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + Delivery_Name+status, na.action = 0)
train$status
unique(train$status)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price +status)
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + status)
rf_model <- randomForest(factor(fraud_status) ~ status)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status))
detach(train)
plot(rf_model, ylim = c(0, 0.5))
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
rankImportance
cancelled_items <- trans %>%
group_by(Category_Level_3, Category_Level_2) %>%
mutate(canc_prop = ifelse(Cancelled_Qty == 0, 0, Cancelled_Qty/Ordered_Qty))
cancelled_items
# Build model
attach(train)
train <- train %>%
group_by(Category_Level_3, Category_Level_2) %>%
mutate(canc_prop = ifelse(Cancelled_Qty == 0, 0, Cancelled_Qty/Ordered_Qty))
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop)
detach(train)
plot(rf_model, ylim = c(0, 0.5))
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
# Remove unecessary columns
train <- train %>%
mutate(ship_status = ifelse(Postcode_Billing_Address != Postcode_Shipping_Address, "mismatch", "fine")) %>%
select(-c(Order_Number, Released_by, Internal.RC, Defence.Status, GBP.Amount, Account_Key, Empty1, Empty2))
source('~/Documents/university/MSc/fundamental/fraud/R/fraud_rf.R')
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
data$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
train$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
library(lubridate)
train$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
library(lubridate)
train$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
weekday <- c("Mon", "Tue", "Wed", "Thu", "Fri")
for (i in 1:length(train$Day)){
if (any(weekday==train$Day[i]) == TRUE){
train$Weekday[i] = 1
}
else{
train$Weekday[i] = 0
}
}
if (any(weekday==train$Day[i])){
train$Weekday[i] = 1
}
weekday <- c("Mon", "Tue", "Wed", "Thu", "Fri")
for (i in 1:length(train$Day)){
if (any(weekday==train$Day[i]) == TRUE){
train$Weekday[i] = 1
}
else{
train$Weekday[i] = 0
}
}
train$Weekday <- 0
library(lubridate)
train$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
weekday <- c("Mon", "Tue", "Wed", "Thu", "Fri")
train$Weekday <- 0
for (i in 1:length(train$Day)){
if (any(weekday==train$Day[i]) == TRUE){
train$Weekday[i] = 1
}
else{
train$Weekday[i] = 0
}
}
train$Day[1:5]
chargeback <- fread("MAIN_transaction_data.csv")
fraud_trans <- fread("MAIN_chargeback_data.csv")
customers <- read.csv("MAIN_customer_data.csv")
chargeback$Order_Date_Key <- ymd(chargeback$Order_Date_Key)
source('~/Documents/university/MSc/fundamental/fraud/R/fraud_rf.R')
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
# Remove unecessary columns
train <- train %>%
mutate(ship_status = ifelse(Postcode_Billing_Address != Postcode_Shipping_Address, "mismatch", "fine")) %>%
select(-c(Order_Number, Released_by, Internal.RC, Defence.Status, GBP.Amount, Account_Key, Empty1, Empty2))
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status))
factor(fraud_status)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
source('~/Documents/university/MSc/fundamental/fraud/R/fraud_rf.R')
# Remove unecessary columns
train <- train %>%
mutate(ship_status = ifelse(Postcode_Billing_Address != Postcode_Shipping_Address, "mismatch", "fine")) %>%
select(-c(Order_Number, Released_by, Internal.RC, Defence.Status, GBP.Amount, Account_Key, Empty1, Empty2))
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
detach(train)
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
detach(train)
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
plot(rf_model, ylim = c(0, 0.5))
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
colnames()
colnames(train)
unique(train$fraud_status)
rf_model <- randomForest(fraud_status ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
library(data.table)
library(dplyr)
library(randomForest)
setwd("/home/tpin3694/Documents/university/MSc/fundamental/THG-Jarvis/")
chargeback <- fread("MAIN_transaction_data.csv")
fraud_trans <- fread("MAIN_chargeback_data.csv")
customers <- read.csv("MAIN_customer_data.csv")
colnames(chargeback)
colnames(fraud_trans)
colnames(fraud_trans)[1] <- "Order_Number"
colnames(customers)
chargeback$Order_Date_Key <- ymd(chargeback$Order_Date_Key)
samp <- runif(n = 0.01*nrow(chargeback), min = 1, max = dim(chargeback)[1])
fraud_accounts <- customers
fraud <- fraud_trans %>%
inner_join(chargeback, by = "Order_Number")
to_app <- fraud_trans %>%
right_join(chargeback, by = "Order_Number") %>%
filter(is.na(Internal.RC) == TRUE) %>%
sample_n(size = nrow(chargeback)*0.01)
# 1 indicates fraud, 0 not legitimate
train <- rbindlist(list(to_app, fraud))
train$fraud_status <- ifelse(is.na(train$Internal.RC) == TRUE, 0, 1)
del_key <- fread("delivery_option_lookup.csv")
colnames(del_key) <- c("Delivery_Option_Type_Key", "Delivery_Name")
trans_del <- trans %>%
left_join(del_key) %>%
group_by(Delivery_Name) %>%
summarise(n = n())
train <- train %>%
left_join(del_key, by = "Delivery_Option_Type_Key")
train <- train %>%
group_by(Account_Key) %>%
mutate(pay_key_num = as.numeric(Order_Payment_Status_Key),
num_valid = length(which(pay_key_num == 0)),
count = n(),
prop = num_valid/count,
status = ifelse(prop <= 0.25 & count > 2, "questionable",
ifelse(prop <= 0.25 & count <= 2, "insufficient",
ifelse(prop > 0.25 & prop < 0.75, "adequate", "fine"))))
train <- train %>%
group_by(Category_Level_3, Category_Level_2) %>%
mutate(canc_prop = ifelse(Cancelled_Qty == 0, 0, Cancelled_Qty/Ordered_Qty))
library(lubridate)
train$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
weekday <- c("Mon", "Tue", "Wed", "Thu", "Fri")
train$Weekday <- 0
for (i in 1:length(train$Day)){
if (any(weekday==train$Day[i]) == TRUE){
train$Weekday[i] = 1
}
else{
train$Weekday[i] = 0
}
}
# Remove unecessary columns
train <- train %>%
mutate(ship_status = ifelse(Postcode_Billing_Address != Postcode_Shipping_Address, "mismatch", "fine")) %>%
select(-c(Order_Number, Released_by, Internal.RC, Defence.Status, GBP.Amount, Account_Key, Empty1, Empty2))
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
factor(fraud_status)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status))
?randomForest
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday), na.action = na.omit)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price +  canc_prop + factor(ship_status) +
factor(Weekday), na.action = na.omit)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop +
factor(Weekday), na.action = na.omit)
factor(fraud_status)
detach(train)
plot(rf_model, ylim = c(0, 0.5))
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
rankImportance
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop +
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop +
factor(Weekday))
detach(train)
plot(rf_model, ylim = c(0, 0.5))
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
table(bill_ship_match$status)
train <- train %>%
mutate(ship_status = ifelse(Postcode_Billing_Address != Postcode_Shipping_Address, "mismatch", "fine")) %>%
group_by(Category_Level_3, Category_Level_2) %>%
mutate(canc_prop = ifelse(Cancelled_Qty == 0, 0, Cancelled_Qty/Ordered_Qty))
library(lubridate)
train$Day <- wday(train$Date.Logged, label = TRUE, abbr = TRUE)
weekday <- c("Mon", "Tue", "Wed", "Thu", "Fri")
train$Weekday <- 0
for (i in 1:length(train$Day)){
if (any(weekday==train$Day[i]) == TRUE){
train$Weekday[i] = 1
}
else{
train$Weekday[i] = 0
}
}
# Remove unecessary columns
train <- train %>%
select(-c(Order_Number, Released_by, Internal.RC, Defence.Status, GBP.Amount, Account_Key, Empty1, Empty2))
# Build model
attach(train)
# Remove unecessary columns
train <- train %>%
select(-c(Order_Number, Released_by, Internal.RC, Defence.Status, GBP.Amount, Account_Key, Empty1, Empty2))
# Build model
attach(train)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor()
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday))
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop + factor(ship_status) +
factor(Weekday), na.action = na.omit)
rf_model <- randomForest(factor(fraud_status) ~ Delivery_Option_Type_Key + Payment_Method_Key + Payment_Provider_Key +
Ordered_Qty + Product_Charge_Price + factor(status) + canc_prop +
factor(Weekday))
detach(train)
plot(rf_model, ylim = c(0, 0.5))
legend('topright', colnames(rf_model$err.rate), col=1:3, fill=1:3)
# Variable Importance
var_importance <- importance(rf_model)
varImportance <- data.frame(Variables = row.names(var_importance),
Importance = round(var_importance[ ,'MeanDecreaseGini'],2))
rankImportance <- varImportance %>%
mutate(Rank = paste0('#',dense_rank(desc(Importance))))
ggplot(rankImportance, aes(x = reorder(Variables, Importance),
y = Importance, fill = Importance)) +
geom_bar(stat='identity') +
geom_text(aes(x = Variables, y = 0.5, label = Rank),
hjust=0, vjust=0.55, size = 4, colour = 'red') +
labs(x = 'Variables') +
coord_flip() +
theme_minimal()
x <- read.csv("/home/tpin3694/Documents/university/MSc/dataMining/group_work/emotion_recognition/data/mnist/split
/train.csv")
x <- read.csv("/home/tpin3694/Documents/university/MSc/dataMining/group_work/emotion_recognition/data/mnist/split/train.csv")
nrow(x)
ncol(x)
setwd("/home/tpin3694/Documents/university/MSc/dataMining/group_work/emotion_recognition/data/mnist/tsne/")
digits <- read.csv("pca_data.csv")
smp_size <- floor(0.7*nrow(digits))
set.seed(123)
train_ind <- sample(seq_len(nrow(digits)), size = smp_size)
train <- digits[train_ind, ]
test <- digits[-train_ind, ]
source('~/Documents/university/MSc/dataMining/group_work/emotion_recognition/other/dataSplit.R')
source('~/Documents/university/MSc/dataMining/group_work/emotion_recognition/other/dataSplit.R')
source('~/Documents/university/MSc/dataMining/group_work/emotion_recognition/other/dataSplit.R')
source('~/Documents/university/MSc/dataMining/group_work/emotion_recognition/other/dataSplit.R')
source('~/Documents/university/MSc/dataMining/group_work/emotion_recognition/other/dataSplit.R')
